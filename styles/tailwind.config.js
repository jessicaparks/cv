const colors = require('material-ui-colors')

module.exports = {
  theme: {
    colors: colors,
    extend: {
      colors: {
        change: "transparent",
      }
    }
  },
  variants: {},
  plugins: [],
};
